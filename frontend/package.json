{
  "name": "translate-frontend",
  "version": "1.0.0",
  "description": "Frontend for audio translation app",
  "main": "main.js",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'No build step required for static frontend'"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.12.10",
    "@ffmpeg/util": "^0.12.1"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
